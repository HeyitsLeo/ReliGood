providers = ["node"]

[phases.setup]
nixPkgs = ["..."]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @zamgo/admin build", "pnpm --filter @zamgo/storefront build"]
