[build]
builder = "nixpacks"
buildCommand = "pnpm install"

[deploy]
startCommand = "node --loader tsx/esm apps/backend/src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
