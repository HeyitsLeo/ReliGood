[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm db:migrate && node --import tsx apps/backend/src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
